using System;

namespace Nucs.JsonSettings.Examples {
    public class Program {
        [STAThread]
        public static void Main(string[] args) {
            //uncomment the example you want to run
            
            //JsonProgram.Run(args);
            //EncryptedProgram.Run(args);
            //SettingsBagProgram.Run(args);
            //EncryptedSettingsBagProgram.Run(args);
            //ComplexObjectsProgram.Run(args);
            //INotifyPropertyChangedExample.Run(args);
        }
    }
}